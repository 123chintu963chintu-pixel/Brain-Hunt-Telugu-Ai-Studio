export const metadata = {
  title: "Brain Hunt Telugu AI Studio",
  description: "AI Studio",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
